## Introduction

This directory contains CQL libraries that implement the INDICATE quality indicators (and helper libraries).
All CQL code in this directory is periodically updated from an upstream repository.
For this reason, any changes or proposals for changes should be made in that upstream repository, not here.
